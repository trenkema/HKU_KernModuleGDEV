#pragma once

#include <string>
#include <vector>

using namespace std;

class Inventory {
public:
    Inventory();
    //void AddItem(string item);
    vector <string> inventoryList;
private:
    //vector<string> inventoryList;
};